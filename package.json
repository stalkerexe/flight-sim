{
  "name": "flight-sim-explorer",
  "version": "0.1.0",
  "type": "module",
  "description": "Post-apocalyptic open-world browser flight simulator — procedural terrain, floating-origin engine core.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "three": "^0.168.0",
    "simplex-noise": "^4.0.3"
  },
  "devDependencies": {
    "typescript": "^5.6.2",
    "vite": "^6.0.0",
    "@types/three": "^0.168.0"
  }
}
